notifications:
  email: false

language: python
python:
  - "3.4"
  - "3.5"
install:
  - pip install -r requirements.txt
  - pip install coveralls
script:
  coverage run WarDrivePiCar/__main__.py
after_success:
  coveralls
  
branches:
  only:
      - master
      - dev
