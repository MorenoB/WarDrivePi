notifications:
  email: false

language: python
python:
  - "2.7"
install:
  - pip install -r requirements.txt
  - pip install axel==src-0.0.5
  - python setup.py install
script:
    - py.test --cov=WarDrivePiCar
after_success:
    - coveralls
  
branches:
  only:
      - master
      - dev
